.panel.panel-primary
  .panel-heading
    h1 Lista de especialidades
  .panel-body
    table.table.table-striped
      tr
        td Nombre

      - @specialties.each do |specialty|
        tr
          td
            strong = link_to specialty.name, specialty_bodyparts_path(specialty)
          td
            = link_to 'Editar', edit_specialty_path(specialty), class: 'btn btn-warning btn-xs'
            = link_to 'Eliminar', edit_specialty_path(specialty), class: "btn btn-danger btn-xs"
  .panel-footer
    = link_to 'New Specialty', new_specialty_path, class: "btn btn-primary"
   
